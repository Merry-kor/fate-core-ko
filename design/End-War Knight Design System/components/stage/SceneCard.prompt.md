**SceneCard** — a scene thumbnail in the director rail. The `active` scene shows an ON AIR badge + gold frame and becomes the stage backdrop.

```jsx
<SceneCard chapter="제2장" name="야간 화물열차 위" thumbnail={trainImg} active onActivate={...} />
```
