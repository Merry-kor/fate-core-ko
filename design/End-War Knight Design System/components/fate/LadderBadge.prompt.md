**LadderBadge** — a Fate ladder rank as "+N 단어"; also exports `LADDER` map + `ladderLabel(rank)`.

```jsx
<LadderBadge rank={4} />        {/* +4 대단함 */}
<LadderBadge rank={-2} />       {/* -2 엉망 */}
<LadderBadge rank={2} showLabel={false} />
```
