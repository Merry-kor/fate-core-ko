**FateDie** — one Fudge die face. Render four for a roll. `size` is deliberately modest so dice print cleanly in the PDF log.

```jsx
<FateDie value={1} /><FateDie value={0} /><FateDie value={-1} /><FateDie value={1} />
```
