**RollCard** — the chat card for a 4dF roll: dice + modifier + ladder total + outcome. Pass `difficulty` to compute 압도적 성공 / 성공 / 동점 / 실패.

```jsx
<RollCard actor="이반 이바노비치" skill="강하게" action="극복"
  dice={[1, 1, 0, -1]} modifier={4} difficulty={2} />
```
