**IconButton** — compact square action for list rows; tone tints the hover state.

```jsx
<IconButton tone="gold" title="굴리기"><i className="fa fa-dice-d20" /></IconButton>
<IconButton tone="crimson" title="삭제"><i className="fa fa-trash" /></IconButton>
```
